import React from "react";
import Router from "./shared/Router";

const App = () => {
  return <Router />;
};

export default App;

//0. 컴포넌트폴더, 페이지폴더, 리덕스/config,modules 폴더, shared 폴더와 파일 생성및 정리(스케치)
//1. App.js를 라우터에 연결.//confisStore
